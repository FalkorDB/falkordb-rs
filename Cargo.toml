[package]
name = "falkordb"
version = "0.1.0"
edition = "2021"
description = "A FalkorDB Rust client"
license = "SSPL-1.0"


[dependencies]
anyhow = { version = "1.0.86", default-features = false, features = ["std"] }
log = { version = "0.4.21", default-features = false }
parking_lot = { version = "0.12.3", default-features = false, features = ["deadlock_detection"] }
redis = { version = "0.25.4", default-features = false, optional = true, features = ["sentinel"] }
regex = { version = "1.10.4", default-features = false, features = ["std", "perf", "unicode-bool", "unicode-perl"] }
thiserror = "1.0.61"
tracing = { version = "0.1.40", default-features = false, features = ["std", "attributes"] }
url-parse = "1.0.8"

[features]
default = ["redis"]
native-tls = ["redis/tls-native-tls"]
rustls = ["redis/tls-rustls"]
redis = ["dep:redis"]
